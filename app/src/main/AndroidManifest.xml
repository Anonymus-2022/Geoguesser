<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="fspreng"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Explo"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StartBild">
            <!-- Weitere Activity-Konfigurationen, z. B. Absichtsfilter, hier einfügen -->
        </activity>
        <activity android:name=".GameView">
            <!-- Weitere Einstellungen -->
        </activity>
        <activity android:name=".SpielAnleitung">
        </activity>
        <activity android:name=".SetTip">

        </activity>
        <activity android:name=".ResultView">
        </activity>
    </application>

</manifest>